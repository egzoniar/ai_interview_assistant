# AI Interview Assistant Configuration

# API Keys (OpenAI now optional since we're using local Ollama)
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/google-cloud-credentials.json
OPENAI_API_KEY=your_openai_api_key_here  # Optional - only needed if using OpenAI fallback

# Local AI Configuration (NEW)
OLLAMA_MODEL=gemma3:4b-it-qat  # Options: gemma3:1b-it-qat (fastest), gemma3:4b-it-qat (balanced), gemma3:27b-it-qat (best quality)

# Audio Configuration
SAMPLE_RATE=16000
CHUNK_SIZE=4096
CHANNELS=1

# Speech Detection (Optimized for responsiveness)
MIN_SPEECH_DURATION=1.5        # Minimum seconds of speech before processing
MAX_SPEECH_DURATION=30.0       # Maximum seconds before forced processing
FINAL_SILENCE_THRESHOLD=2.5    # Silence duration to confirm speech completion
MIN_AUDIO_LEVEL=0.005          # Audio sensitivity level (lower = more sensitive)

# Speaker Configuration  
USER_SPEAKER_LABEL=1           # Your speaker label to filter out your voice

# Performance Notes:
# - With local Ollama, AI responses should be 3-5x faster than OpenAI
# - gemma3:1b-it-qat: ~0.5-1s response time (very fast, good for quick responses)
# - gemma3:4b-it-qat: ~1-2s response time (recommended balance)  
# - gemma3:27b-it-qat: ~3-5s response time (highest quality)

# Troubleshooting:
# - Still getting cut-off questions? Increase FINAL_SILENCE_THRESHOLD to 3.5
# - Responses too slow? Use gemma3:1b-it-qat model
# - Want higher quality? Use gemma3:27b-it-qat model
# - Ollama not responding? Check: ollama serve 